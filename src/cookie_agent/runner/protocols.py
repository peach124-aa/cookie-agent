"""Protocols specific to the Inference Runner."""

# Runner specific protocols can be defined here if the orchestration needs to
# expose formal contracts to higher-level managers. Currently, all dependencies
# are drawn directly from core.protocols.
